# Detector 训练管理系统使用说明

## 目录

1. [系统简介](#系统简介)
2. [环境配置](#环境配置)
3. [启动系统](#启动系统)
4. [界面概览](#界面概览)
5. [训练配置模块](#训练配置模块)
6. [推理测试模块](#推理测试模块)
7. [实时流检测模块](#实时流检测模块)
8. [模型管理模块](#模型管理模块)
9. [常见问题](#常见问题)

---

## 系统简介

Detector 训练管理系统是一套基于 YOLOv7 架构的目标检测模型训练和推理平台，提供可视化 Web 界面，支持：

- **训练配置**：数据配置、训练参数设置、训练过程监控
- **推理测试**：视频/图像推理、批量处理、结果导出
- **实时流检测**：RTSP 视频流实时目标检测
- **模型管理**：模型列表、导出 ONNX、删除操作

---

## 环境配置

### 方式一：从压缩包安装（推荐）

1. **解压 Conda 环境压缩包**
   - 将获得的 `conda_env.tar.gz` 或类似压缩包解压至 Conda 的 envs 文件夹
   - 解压路径：`C:\Users\你的用户名\anaconda3\envs\`

2. **激活环境**
   ```bash
   conda activate 你的环境名
   ```

3. **验证环境**
   ```bash
   python --version  # 应显示 Python 版本
   conda list        # 查看已安装的包
   ```

### 方式二：手动安装依赖

```bash
# 创建新环境
conda create -n detector python=3.10 -y
conda activate detector

# 安装 PyTorch（根据你的 CUDA 版本选择）
# CUDA 11.8
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# CUDA 12.8
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# 安装其他依赖
pip install flask opencv-python pillow pyyaml tqdm numpy pandas scipy matplotlib seaborn
pip install tensorboard
```

---

## 启动系统

### 启动命令

在 `yolo_train_tool` 的**上级目录**进入终端命令行，执行：

```bash
python -m detector_gui.cli
```

### 自定义端口和地址

```bash
# 指定端口（默认 5000）
python -m detector_gui.cli --port 8080

# 指定监听地址（默认 0.0.0.0，允许外部访问）
python -m detector_gui.cli --host 127.0.0.1

# 组合使用
python -m detector_gui.cli --port 8080 --host 0.0.0.0

# 启用调试模式（开发用）
python -m detector_gui.cli --debug
```

### 访问界面

启动后，终端会显示访问地址：

```
==================================================
  Detector 训练管理界面
  访问地址: http://127.0.0.1:5000
==================================================
```

打开浏览器访问 `http://127.0.0.1:5000` 即可进入系统。

---

## 界面概览

系统主界面分为四个主要模块，通过顶部导航栏切换：

| 模块 | 功能 |
|------|------|
| 训练配置 | 数据配置、训练参数设置、启动/停止训练 |
| 推理测试 | 视频/图像目标检测、批量推理 |
| 实时流检测 | RTSP 视频流实时检测 |
| 模型管理 | 训练模型列表、导出 ONNX、删除 |

---

## 训练配置模块

### 数据配置

系统支持两种数据配置方式：

#### 方式一：选择 YAML 文件

1. 在 `data` 下拉框中选择已有的 `data.yaml` 配置文件
2. 或点击「浏览」按钮选择自定义配置文件
3. 配置文件应包含 `train`、`val`、`nc`、`names` 等字段

**data.yaml 示例**：
```yaml
train: ./dataset/train/images
val: ./dataset/val/images
nc: 2
names: ['military vehicle', 'civilian vehicle']
```

#### 方式二：从图片目录生成

1. 点击「从图片目录生成」
2. 在「图片目录路径」选择包含图片的文件夹
3. 设置「类别数量」和「类别名称」（逗号分隔）
4. 点击「生成数据集」

### 训练参数设置

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 训练轮数 (epochs) | 训练迭代次数 | 20 |
| 批次大小 (batch-size) | 每批训练样本数 | 32 |
| 训练图像尺寸 | 训练时图像分辨率 | 1024 |
| 测试图像尺寸 | 测试时图像分辨率 | 1024 |
| 工作线程数 (workers) | 数据加载线程数 | 16 |

### 高级参数

点击「显示高级参数」可展开更多设置：

| 参数 | 说明 |
|------|------|
| 预训练权重 | 可选择 pretrained weights |
| 优化器 | SGD / Adam / AdamW |
| 学习率 | 初始学习率 |
| 权重衰减 | 正则化参数 |
| 启用 mosaic | 数据增强 |
| 启用 mixup | 数据增强 |

### 设备选择

点击「选择设备」按钮，在弹窗中选择：

- **GPU**：如果有 NVIDIA 显卡，选择对应的 GPU 设备
- **CPU**：如果没有 GPU 或 GPU 不可用

### 启动和监控训练

1. **配置完成后**，点击「开始训练」按钮
2. 系统会创建训练输出目录 `runs/train/exp*`
3. **训练日志**实时显示在下方的日志区域
4. 点击「停止训练」可中断训练过程

**训练输出**：
- 训练过程日志实时刷新
- 训练完成后自动保存模型权重到 `runs/train/exp*/weights/best.pt`
- 可在模型管理模块查看和管理训练结果

---

## 推理测试模块

### 模型选择

1. **选择推理方式**：
   - **快速推理**：使用 YOLO 模型，速度快
   - **大模型推理**：使用 Qwen 等视觉语言模型，精度高

2. **选择模型文件**：
   - 快速推理：选择 `.pt` 格式的 YOLO 权重文件
   - 大模型推理：选择包含模型文件的文件夹

### 输入设置

#### 视频推理

1. 选择「视频文件」作为输入类型
2. 点击「选择视频」上传视频文件
3. 设置「帧间隔」（每隔多少帧处理一帧）
4. 设置「最大帧数」（最多处理多少帧）

#### 图片文件夹推理

1. 选择「图片文件夹」作为输入类型
2. 点击「浏览」选择包含图片的文件夹
3. 系统会自动处理文件夹中的所有图片

### 推理参数

| 参数 | 说明 |
|------|------|
| 置信度阈值 | 只显示/保存置信度高于此值的目标 |
| 忽略类别ID | 输入类别 ID，多个用逗号分隔，被忽略的目标不显示不保存 |
| 只保存有目标图像 | 勾选后，只保存检测到目标的图像 |

### 推理结果

1. 点击「开始推理」
2. 推理进度实时显示
3. 结果展示区显示检测到的目标
4. 每帧图像的检测结果：
   - 绿色边界框标注目标位置
   - 显示类别名称和置信度
   - 标签文件保存在输出目录的 `labels` 子文件夹

**输出文件结构**：
```
inference_result/
├── images/           # 检测后的图像
│   ├── frame_000000.jpg
│   └── frame_000001.jpg
└── labels/           # YOLO 格式标签
    ├── frame_000000.txt
    └── frame_000001.txt
```

**标签文件格式**（YOLO txt）：
```
类别ID cx cy w h 置信度
0 0.512345 0.523456 0.123456 0.234567 0.95
```

### 视频帧命名说明

视频推理时，帧图像命名包含时间信息：`frame_序号_秒s_毫秒ms.jpg`

例如：`frame_000000_00s_000ms.jpg` 表示第 1 帧，视频中 0 秒 0 毫秒位置。

---

## 实时流检测模块

### 连接视频流

1. 在「RTSP 地址」输入视频流地址
   ```
   rtsp://用户名:密码@IP地址:端口/路径
   ```
   例如：
   ```
   rtsp://admin:admin123@192.168.1.100:554/stream1
   ```

2. 点击「连接视频流」

3. 连接成功后：
   - 实时画面区域显示视频流
   - 检测结果实时显示在右侧
   - 当前帧画面会高亮检测到的目标

### 操作功能

| 按钮 | 功能 |
|------|------|
| 断开连接 | 断开当前视频流连接 |
| 保存当前帧 | 将当前检测画面保存到本地 |
| 发送当前帧 | 将当前帧发送到推理模块进行详细分析 |

### 录制功能

1. 点击「开始录制」可录制视频流
2. 录制过程中会显示录制时长
3. 点击「停止录制」结束录制
4. 录制的视频保存在 `recordings/` 目录

---

## 模型管理模块

### 查看模型列表

1. 进入「模型管理」模块
2. 系统自动扫描 `runs/train/` 目录下的训练结果
3. 显示模型列表，包括：
   - 模型名称（实验名）
   - 训练时间
   - 训练轮数
   - 模型大小

### 模型详情

点击模型卡片可查看详细信息：

- 训练配置参数
- 数据集信息
- 训练日志摘要
- mAP@0.5 和 mAP@0.5:0.95 指标

### 导出 ONNX

1. 点击模型卡片的「导出 ONNX」按钮
2. 在弹窗中设置导出参数：
   - **宽度/高度**：导出模型的输入尺寸（默认 1024）
   - **导出格式**：
     - GPU 格式：不含 grid，适用于 GPU 推理
     - NPU 格式：含 grid，适用于 NPU/端侧推理
3. 点击「开始导出」
4. 导出完成后，显示 ONNX 文件路径

### 删除模型

1. 点击模型卡片的「删除」按钮
2. 确认删除操作
3. 模型及其所有输出文件将被删除

---

## 常见问题

### 1. 启动时报错 "ModuleNotFoundError"

**原因**：缺少必要的 Python 包

**解决**：
```bash
pip install flask opencv-python pillow pyyaml tqdm numpy pandas scipy matplotlib seaborn
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

### 2. 训练时 GPU 不可用

**检查 GPU 是否被识别**：
```python
import torch
print(torch.cuda.is_available())
print(torch.cuda.device_count())
print(torch.cuda.get_device_name(0))
```

**解决**：
- 确保安装了支持 CUDA 的 PyTorch 版本
- 确保 NVIDIA 驱动已正确安装
- 或在界面中选择 CPU 设备

### 3. 推理速度慢

**优化建议**：
- 视频推理时增大「帧间隔」，减少处理帧数
- 使用 GPU 进行推理
- 减小「最大帧数」限制
- 大模型推理耗时较长属正常现象

### 4. 无法连接 RTSP 视频流

**检查项**：
- RTSP 地址格式是否正确
- 网络是否能访问摄像头/服务器
- 摄像头是否支持 RTSP 协议
- 尝试使用 VLC 播放器验证 RTSP 地址

### 5. 导出 ONNX 失败

**常见原因**：
- CUDA 版本不兼容
- 模型包含不支持的操作
- 图像尺寸设置不合理

**解决**：
- 尝试使用较小的图像尺寸（如 640）
- 使用 GPU 格式导出
- 检查 PyTorch 和 ONNX 版本兼容性

### 6. 内存不足 (OOM)

**解决**：
- 减小「批次大小」
- 减小「图像尺寸」
- 关闭其他占用 GPU 的程序
- 大模型推理需要较大显存（约 8GB+）

### 7. 浏览器无法访问界面

**检查项**：
- 服务是否已启动（查看终端输出）
- 端口是否被占用
- 防火墙是否阻止了端口
- 地址是否输入正确

**尝试**：
```bash
# 使用其他端口
python -m detector_gui.cli --port 8080

# 然后访问 http://127.0.0.1:8080
```

---

## 技术支持

如遇到其他问题，请提供以下信息：

1. 错误完整的报错信息
2. 操作系统和 Python 版本
3. GPU 型号和 CUDA 版本
4. 操作步骤描述